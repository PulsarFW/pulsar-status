name: Create release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Get latest code
        uses: actions/checkout@v4

      - name: Bundle files
        run: |
          mkdir -p ./temp/pulsar-status
          cp ./{README.md,fxmanifest.lua} ./temp/pulsar-status
          cp -r ./{client,server} ./temp/pulsar-status
          cd ./temp && zip -r ../pulsar-status.zip ./pulsar-status

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          name: "PULSAR-STATUS ${{ github.ref_name }}"
          body: |
            ![Pulsar Framework](https://r2.fivemanage.com/GPYOH8Hq4GPyAY7czrgLe/pulsarbanner.png)

            ## PULSAR-STATUS ${{ github.ref_name }}

            Player needs and status effect management for Pulsar Framework.

            ### Installation
            1. Download `pulsar-status.zip` below
            2. Extract into your server's `resources` folder
            3. Add `ensure pulsar-status` to your `server.cfg`

            ---
            ![Pulsar Framework](https://img.shields.io/badge/Pulsar-Framework-7c3aed?style=flat-square)
            ![Built for FiveM](https://img.shields.io/badge/Built_for-FiveM-F40552?style=flat-square)
          files: pulsar-status.zip
          prerelease: false
